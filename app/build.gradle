plugins { id 'com.android.application' }

android {
    namespace 'com.leostartup.loopdots'
    compileSdk 35
    defaultConfig {
        // Preserve the current app ID; signature must match when installing updates.
        applicationId 'com.leostartup.loopdots.v2'
        minSdk 26
        targetSdk 35
        versionCode 6
        versionName '1.5'
    }
}
