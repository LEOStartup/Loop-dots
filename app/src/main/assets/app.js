'use strict';
const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const paths={plus:'M12 4v16M4 12h16',grid:'M3 3h7v7H3zM14 3h7v7h-7zM3 14h7v7H3zM14 14h7v7h-7z',week:'M4 6h16M4 12h16M4 18h16',year:'M4 4h16v16H4z',stats:'M5 20V10M12 20V4M19 20v-7',settings:'M9 3h6l1 3 3 1 2 5-2 5-3 1-1 3H9l-1-3-3-1-2-5 2-5 3-1zM9 12a3 3 0 1 0 6 0a3 3 0 1 0-6 0',check:'m5 12 4 4L20 5',close:'m6 6 12 12M6 18 18 6',left:'m15 5-7 7 7 7',right:'m9 5 7 7-7 7',down:'m5 9 7 7 7-7',calendar:'M4 5h16v16H4zM8 2v6M16 2v6M4 10h16',note:'M5 5h14M5 10h14M5 15h9M5 20h9',edit:'m14 4 6 6M4 20l5-1L21 7l-5-5L4 14z',share:'M8 12l9-6M8 12l9 6M4 12a2 2 0 1 0 4 0a2 2 0 1 0-4 0M17 5a2 2 0 1 0 4 0a2 2 0 1 0-4 0M17 19a2 2 0 1 0 4 0a2 2 0 1 0-4 0',archive:'M3 3h18v5H3zM5 8v13h14V8M9 12h6',sun:'M12 7a5 5 0 1 0 0 10a5 5 0 1 0 0-10M12 1v2M12 21v2M1 12h2M21 12h2M4 4l2 2M18 18l2 2M20 4-2 2M6 18l-2 2',globe:'M12 2a10 10 0 1 0 0 20a10 10 0 1 0 0-20M2 12h20M12 2c-6 6-6 14 0 20M12 2c6 6 6 14 0 20',backup:'M7 3v18m-4-4 4 4 4-4M17 21V3m-4 4 4-4 4 4',heart:'M12 21 3 12C-3 3 8-1 12 6C16-1 27 3 21 12z',star:'m12 2 3 6 7 1-5 5 1 7-6-3-6 3 1-7-5-5 7-1z',help:'M8 7c0-6 10-6 10 0 0 3-6 3-6 7M12 19v1',bell:'M5 17h14l-2-4V8c0-7-10-7-10 0v5zM10 21h4',fire:'M12 2c3 7 8 8 8 14 0 9-16 9-16 0 0-3 2-5 4-7 0 4 2 5 3 4z',trophy:'M7 3h10v7c0 8-10 8-10 0zM7 5H3v5l4 3M17 5h4v5l-4 3M12 16v5M8 21h8',percent:'M5 19 19 5M5 5h3v3H5zM16 16h3v3h-3z',hash:'M9 3 7 21M17 3 15 21M3 9h18M3 15h18',trash:'M3 6h18M8 6V3h8v3M5 6l1 15h12l1-15',search:'M10 3a7 7 0 1 0 0 14a7 7 0 1 0 0-14M15 15l6 6'};
const icon=n=>`<svg viewBox="0 0 24 24" aria-hidden="true"><path d="${paths[n]||paths.grid}"/></svg>`;
let state,loadError=false;try{state=Core.validate(JSON.parse(window.Android?Android.load():localStorage.getItem('loopdots')||'{"version":3,"habits":[],"settings":{}}'))}catch(e){loadError=true;state={version:3,habits:[],settings:{}}}state.settings??={};state.categories??=['Arte','Estudo','Finanças','Fitness','Nutrição','Saúde','Social','Trabalho','Outro','Manhã','Dia','Noite'];
let page=state.settings.start||'month',selected=state.habits.find(h=>!h.archived)?.id,statYear=new Date().getFullYear(),contextId=null,draft=null,calMonth=new Date(),activeSheet=null;
const english={'Mês':'Month','Semana':'Week','Ano':'Year','Estatísticas':'Statistics','Definições':'Settings','Criar Hábito':'Create habit','Editar Hábito':'Edit habit','Nome':'Name','Descrição':'Description','Guardar':'Save','Salvar':'Save','Cancelar':'Cancel','Concluir':'Complete','Desmarcar':'Uncheck','Calendário':'Calendar','Notas':'Notes','Editar':'Edit','Partilhar':'Share','Arquivar':'Archive','Geral':'General','Tema':'Theme','Idioma':'Language','Exportar/importar hábitos':'Export/import habits','Hábitos arquivados':'Archived habits','Indicar amigos':'Invite friends','Website':'Website','Política de Privacidade':'Privacy Policy','Termos de Serviço':'Terms of Service','Siga-nos':'Follow us','Solicitação de recurso':'Feature request','Erro encontrado':'Report a bug','Avalia-nos':'Rate us','Enviar feedback':'Send feedback','Apoiar o desenvolvedor':'Support the developer','Compartilhar aplicativo':'Share app','Dias concluídos':'Completed days','Taxa de conclusão':'Completion rate','Conclusões mensais':'Monthly completions','Sequência diária atual':'Current daily streak','Melhor sequência diária':'Best daily streak','Sequência semanal atual':'Current weekly streak','Melhor sequência semanal':'Best weekly streak','Sequência mensal atual':'Current monthly streak','Melhor sequência mensal':'Best monthly streak','Nenhum hábito ainda':'No habits yet','Toque em + para criar seu primeiro hábito.':'Tap + to create your first habit.','Lembrete':'Reminder','Categorias':'Categories','Emoji':'Emoji','Conclusões por dia':'Completions per day','Mostrar sequência':'Show streak','Nenhuma nota':'No notes','Adicionar nota':'Add note','Restaurar':'Restore','Excluir definitivamente':'Delete permanently','Começo da semana':'Week starts on','Segunda-feira':'Monday','Domingo':'Sunday','Tela inicial':'Start screen','Vibração':'Haptics','Animações':'Animations','Escuro':'Dark','Claro':'Light','Sistema':'System','Exportar backup':'Export backup','Importar backup':'Import backup','Reordenar hábitos':'Reorder habits','Configurar widgets':'Configure widgets','Fechar':'Close'};
const t=s=>state.settings.lang==='en'?(english[s]||s):s,loc=()=>state.settings.lang==='en'?'en-US':'pt-BR';
const weekdays=()=>state.settings.lang==='en'?['Sun','Mon','Tue','Wed','Thu','Fri','Sat']:['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'];
const get=id=>state.habits.find(h=>h.id===id),active=()=>state.habits.filter(h=>!h.archived),todayKey=()=>Core.key(Core.today());
function persist(){if(loadError){toast('Os dados não puderam ser lidos. A gravação foi bloqueada para protegê-los.');return}if(window.Android)Android.save(JSON.stringify(state));else localStorage.setItem('loopdots',JSON.stringify(state))}
function feedback(){if(state.settings.haptic!==false&&window.Android)Android.haptic()}
function toast(s){$('#toast').textContent=s;$('#toast').style.display='block';setTimeout(()=>$('#toast').style.display='none',2300)}
function appearance(){let light=state.settings.theme==='light'||state.settings.theme==='system'&&matchMedia('(prefers-color-scheme:light)').matches;document.body.classList.toggle('light',light);document.documentElement.style.setProperty('--accent',state.settings.accent||'#56af58');let old=$('#motion');if(old)old.remove();if(state.settings.motion===false){let s=document.createElement('style');s.id='motion';s.textContent='*{animation:none!important;transition:none!important}';document.head.append(s)}}
function head(title,plus=true){return `<header class="page-head"><h1>${t(title)}</h1>${plus?`<button class="add" aria-label="${t('Criar Hábito')}" onclick="editHabit()">${icon('plus')}</button>`:''}</header>`}
function render(){appearance();let tabs=[['month','Mês','grid'],['week','Semana','week'],['year','Ano','year'],['stats','Estatísticas','stats'],['settings','Definições','settings']];$('#nav').innerHTML=tabs.map(([id,label,ic])=>`<button class="${page===id?'active':''}" onclick="navigate('${id}')" aria-label="${t(label)}">${icon(ic)}<span>${t(label)}</span></button>`).join('');if(page==='settings')renderSettings();else if(page==='stats')renderStats();else{$('#app').innerHTML=head(tabs.find(x=>x[0]===page)?.[1]||'Mês')+`<section class="cards ${page}">${active().map(h=>card(h)).join('')}</section>`+(active().length?'':`<div class="empty">${icon('grid')}<h3>${t('Nenhum hábito ainda')}</h3>${t('Toque em + para criar seu primeiro hábito.')}</div>`);bindHolds()}}
function navigate(p){closeSheet();page=p;render();window.scrollTo(0,0)}
function calendarCells(month){let first=new Date(month.getFullYear(),month.getMonth(),1,12),start=state.settings.weekStart===0?0:1;let offset=(first.getDay()-start+7)%7;let n=new Date(month.getFullYear(),month.getMonth()+1,0).getDate();return Array.from({length:Math.ceil((offset+n)/7)*7},(_,i)=>Core.add(first,i-offset))}
function cell(h,d,cls=''){let k=Core.key(d),v=Core.value(h,k);return `<button class="dot ${cls} ${Core.done(h,k)?'done':v?'partial':''} ${k===todayKey()?'today':''} ${h.notes?.[k]?'note':''}" ${d>Core.today()?'disabled':''} data-habit="${h.id}" data-date="${k}" onclick="mark('${h.id}','${k}')" aria-label="${esc(h.name)}, ${k}, ${v}"></button>`}
function heatmap(h,year=null){let end=year?new Date(year,11,31,12):Core.today(),start=year?new Date(year,0,1,12):Core.add(end,-230);start=Core.add(start,-((start.getDay()+6)%7));let ds=[];for(let d=start;d<=end;d=Core.add(d,1))ds.push(new Date(d));return `<div class="year-grid" style="grid-template-columns:repeat(${Math.ceil(ds.length/7)},minmax(0,1fr))">${ds.map(d=>cell(h,d,d>Core.today()?'future':'')).join('')}</div>`}
function card(h){let st=Core.streak(h),name=`<div class="habit-head" data-menu="${h.id}"><span class="avatar ${Core.done(h,todayKey())?'':'off'}">${esc(h.icon||'✓')}</span><div class="habit-label"><strong>${esc(h.name)}</strong>${page==='month'?`<small>${new Date().toLocaleDateString(loc(),{month:'short',year:'numeric'})}</small>`:''}</div>${h.showStreak!==false?`<span class="streak">🔥${st.current}</span>`:''}</div>`;let grid='';if(page==='month')grid=`<div class="month-grid">${calendarCells(Core.today()).map(d=>d.getMonth()===new Date().getMonth()?cell(h,d):'<span class="dot outside"></span>').join('')}</div>`;else if(page==='week'){let start=Core.add(Core.today(),-((Core.today().getDay()-(state.settings.weekStart===0?0:1)+7)%7));grid=`<div class="week-grid">${Array.from({length:7},(_,i)=>{let d=Core.add(start,i);return `<div class="week-cell">${weekdays()[d.getDay()]}${cell(h,d)}</div>`}).join('')}</div>`}else grid=heatmap(h);return `<article class="habit-card" style="--hc:${h.color}" data-card="${h.id}">${name}${grid}</article>`}
function bindHolds(){document.querySelectorAll('[data-menu]').forEach(el=>{el.onclick=e=>openMenu(el.dataset.menu,e.clientX,e.clientY)});document.querySelectorAll('[data-card]').forEach(el=>{let timer;el.onpointerdown=e=>{if(e.target.closest('button'))return;timer=setTimeout(()=>openMenu(el.dataset.card,e.clientX,e.clientY),450)};el.onpointerup=el.onpointercancel=el.onpointerleave=()=>clearTimeout(timer);el.oncontextmenu=e=>{e.preventDefault();openMenu(el.dataset.card,e.clientX,e.clientY)}})}
function mark(id,k=todayKey(),force=false){let h=get(id);if(!h||Core.date(k)>Core.today())return;feedback();if(h.mode==='value'&&!force){numberEntry(id,k);return}h.entries??={};let v=Core.value(h,k),target=Math.max(1,+h.target||1);h.entries[k]=v>=target?0:v+1;persist();render();document.querySelectorAll('[data-habit="'+id+'"][data-date="'+k+'"]').forEach(el=>el.classList.add('recent'));if(activeSheet==='calendar')calendar(id,false)}
function openMenu(id,x=innerWidth-30,y=180){contextId=id;let h=get(id);let options=[[Core.done(h,todayKey())?'Desmarcar':'Concluir','check',()=>{closeSheet();mark(id)}],['Calendário','calendar',()=>calendar(id)],['Notas','note',()=>notes(id)],['Editar','edit',()=>editHabit(id)],['Partilhar','share',()=>shareHabit(id)],['Arquivar','archive',()=>{h.archived=true;persist();closeSheet();render();toast(t('Arquivar'))}]];$('#overlay').innerHTML='<div class="scrim" onclick="closeSheet()"></div><div class="menu"></div>';let menu=$('.menu');menu.style.left=Math.min(Math.max(8,x-170),innerWidth-216)+'px';menu.style.top=Math.min(Math.max(10,y-20),innerHeight-350)+'px';options.forEach(([label,ic,fn])=>{let b=document.createElement('button');b.innerHTML=t(label)+icon(ic);b.onclick=fn;menu.append(b)});activeSheet='menu'}
function closeSheet(){if(window.Android)Android.overlay(false);$('#overlay').innerHTML='';activeSheet=null}
function sheet(title,html,kind='generic'){if(window.Android)Android.overlay(true);$('#overlay').innerHTML=`<div class="scrim" onclick="closeSheet()"></div><section class="sheet" role="dialog" aria-modal="true"><div class="sheet-title"><h2>${esc(t(title))}</h2><button class="round" onclick="closeSheet()" aria-label="${t('Fechar')}">${icon('close')}</button></div>${html}</section>`;activeSheet=kind;let panel=$('.sheet'),sy=0;panel.addEventListener('touchstart',e=>{sy=e.touches[0].clientY},{passive:true});panel.addEventListener('touchend',e=>{if(panel.scrollTop===0&&e.changedTouches[0].clientY-sy>130&&!e.target.closest('input,textarea,button'))closeSheet()},{passive:true})}
const colors=['#ef4444','#f97316','#f59e0b','#84cc16','#22c55e','#10b981','#14b8a6','#06b6d4','#0ea5e9','#3b82f6','#6366f1','#8b5cf6','#a855f7','#d946ef','#ec4899','#f43f5e'];
function editHabit(id){let h=get(id);draft=h?JSON.parse(JSON.stringify(h)):{id:'h_'+(crypto.randomUUID?crypto.randomUUID():Date.now()),name:'',description:'',color:colors[0],icon:'💪',target:1,showStreak:true,entries:{},notes:{},created:todayKey(),categories:[],reminderDays:[],reminderTime:'12:00',schedule:[],mode:'step',type:'build'};editForm()}
function captureDraft(){if(!draft||!$('#name'))return;draft.name=$('#name').value;draft.description=$('#description').value;draft.reminderTime=$('#reminderTime').value;draft.goalCount=+$('#goalCount').value||1;draft.goalPeriod=$('#goalPeriod').value;draft.mode=$('#entryMode').value;draft.type=$('#habitType').value}
function editForm(){let h=draft;sheet(get(h.id)?'Editar Hábito':'Criar Hábito',`<form class="form" onsubmit="event.preventDefault();saveHabit()"><input class="field" id="name" aria-label="${t('Nome')}" placeholder="${t('Nome')}" maxlength="120" value="${esc(h.name)}" required><input class="field" id="description" aria-label="${t('Descrição')}" placeholder="${t('Descrição')}" maxlength="500" value="${esc(h.description)}"><div class="color-grid">${colors.map(c=>`<button type="button" class="swatch ${h.color===c?'selected':''}" style="background:${c}" aria-label="${c}" onclick="captureDraft();draft.color='${c}';editForm()"></button>`).join('')}</div><button type="button" class="field row between" onclick="captureDraft();emojiPicker()"><span>${t('Emoji')}</span><span style="font-size:25px">${esc(h.icon)}</span></button><button type="button" class="field" style="text-align:left" onclick="captureDraft();categoryPicker()">${h.categories?.length?esc(h.categories.join(', ')):t('Categorias')}</button><div class="row"><span class="field">${t('Conclusões por dia')}: ${h.target}</span><button type="button" class="round" onclick="captureDraft();draft.target=Math.max(1,draft.target-1);editForm()">−</button><button type="button" class="round" onclick="captureDraft();draft.target=Math.min(9999,draft.target+1);editForm()">+</button></div><label>${t('Lembrete')}</label><div class="days">${[1,2,3,4,5,6,0].map(day=>`<button type="button" class="${h.reminderDays?.includes(day)?'selected':''}" onclick="captureDraft();draft.reminderDays=draft.reminderDays.includes(${day})?draft.reminderDays.filter(x=>x!==${day}):[...draft.reminderDays,${day}];editForm()">${weekdays()[day]}</button>`).join('')}</div><input id="reminderTime" class="field" type="time" aria-label="Horário" value="${h.reminderTime||'12:00'}"><button type="button" class="row between" onclick="captureDraft();draft.showStreak=!draft.showStreak;editForm()"><span>${t('Mostrar sequência')}</span><span class="switch ${h.showStreak!==false?'on':''}"></span></button><details><summary class="subheading">Opções avançadas</summary><label>Tipo de hábito</label><select class="field" id="habitType"><option value="build" ${h.type!=='quit'?'selected':''}>Criar hábito</option><option value="quit" ${h.type==='quit'?'selected':''}>Largar hábito</option></select><p class="hint">Para largar um hábito, registre os dias em que conseguiu evitá-lo.</p><label>Registrar conclusões</label><select class="field" id="entryMode"><option value="step" ${h.mode!=='value'?'selected':''}>Passo a passo</option><option value="value" ${h.mode==='value'?'selected':''}>Valor personalizado</option></select><label>Meta de sequência</label><p class="hint">Diária: dias consecutivos. Semanal ou mensal: dias concluídos no período.</p><div class="row"><select class="field" id="goalPeriod">${[['none','Nenhuma'],['day','Diária'],['week','Semanal'],['month','Mensal']].map(([v,l])=>`<option value="${v}" ${h.goalPeriod===v?'selected':''}>${l}</option>`).join('')}</select><input class="field" id="goalCount" type="number" min="1" max="365" value="${h.goalCount||1}" aria-label="Meta"></div><label>Dias planejados (todos, se nenhum selecionado)</label><div class="days">${[1,2,3,4,5,6,0].map(day=>`<button type="button" class="${h.schedule?.includes(day)?'selected':''}" onclick="captureDraft();draft.schedule=draft.schedule.includes(${day})?draft.schedule.filter(x=>x!==${day}):[...draft.schedule,${day}];editForm()">${weekdays()[day]}</button>`).join('')}</div></details><div class="sheet-footer"><button class="primary" type="submit">${t('Guardar')}</button></div></form>`,'edit')}
function saveHabit(){captureDraft();if(!draft.name.trim()){toast('Informe o nome');return}draft.name=draft.name.trim();let i=state.habits.findIndex(h=>h.id===draft.id);if(i<0)state.habits.push(draft);else state.habits[i]=draft;selected??=draft.id;persist();if(draft.reminderDays.length&&window.Android)Android.notifications();closeSheet();render()}
const emojiGroups={Esporte:'🏃 🏊 🏋️ 🚴 🧗 🏄 🏂 ⛷️ 🤸 🧘 ⚽ 🏀 🎾 🏐 🥊 🏆 🥇 💪',Saúde:'❤️ 🧠 🦷 🩺 💊 💧 🥗 🍎 🥑 🥦 🥕 🛌 😴 🚭 🚫 ☀️ 🌙',Estudo:'📚 📖 ✍️ 📝 🎓 💻 🎨 🎵 🎹 🎸 🗣️ 🔬 🧪 💡',Vida:'🐕 🐈 🐾 🏠 🧹 🧺 🍳 ☕ 🌱 🌳 🧑‍🍳 💼 💰 💳 📈 ✈️ 🚗 🤝 👨‍👩‍👧 📱 🔥 ⭐ ✅ 🎯'};
function emojiPicker(group='Esporte',query=''){sheet('Emoji',`<input class="field" placeholder="Buscar emoji ou categoria" oninput="filterEmoji(this.value)"><div class="chips" style="margin-top:10px">${Object.keys(emojiGroups).map(g=>`<button class="chip ${g===group?'selected':''}" onclick="emojiPicker('${g}')">${g}</button>`).join('')}</div><div class="emoji-grid" id="emojis">${emojiGroups[group].split(' ').map(e=>`<button onclick="draft.icon='${e}';editForm()">${e}</button>`).join('')}</div><input class="field" placeholder="Ou cole seu emoji" maxlength="20" id="customEmoji"><button class="primary" style="margin-top:12px" onclick="if($('#customEmoji').value.trim())draft.icon=$('#customEmoji').value.trim();editForm()">${t('Guardar')}</button>`,'emoji')}
function filterEmoji(q){let aliases={'correr':'🏃','nadar':'🏊','pular':'🤸','cachorro':'🐕','meditação':'🧘','ler':'📚','livro':'📖','água':'💧','dormir':'😴','cozinhar':'🍳','dinheiro':'💰','trabalho':'💼','fogo':'🔥','coração':'❤️','run':'🏃','swim':'🏊'};let items=Object.entries(emojiGroups).filter(([g])=>g.toLowerCase().includes(q.toLowerCase())).map(x=>x[1]).join(' ');items+=' '+Object.entries(aliases).filter(([k])=>k.includes(q.toLowerCase())).map(x=>x[1]).join(' ');$('#emojis').innerHTML=[...new Set(items.trim().split(' '))].filter(Boolean).map(e=>`<button onclick="draft.icon='${e}';editForm()">${e}</button>`).join('')}
function categoryPicker(){sheet('Categorias',`<div class="chips">${state.categories.map((c,i)=>`<button class="chip ${draft.categories.includes(c)?'selected':''}" onclick="let c=state.categories[${i}];draft.categories=draft.categories.includes(c)?draft.categories.filter(x=>x!==c):[...draft.categories,c];categoryPicker()">${esc(c)}</button>`).join('')}</div><div class="row" style="margin:15px 0"><input class="field" id="newCategory" placeholder="Criar categoria" maxlength="40"><button class="round" onclick="let c=$('#newCategory').value.trim();if(c&&!state.categories.includes(c)){state.categories.push(c);draft.categories.push(c);persist();categoryPicker()}">+</button></div><button class="primary" onclick="editForm()">${t('Guardar')}</button>`,'categories')}
function calendar(id,reset=true){let h=get(id);contextId=id;if(reset)calMonth=Core.today();let start=state.settings.weekStart===0?0:1;sheet('Calendário',`<div style="--hc:${h.color}"><div class="year-nav"><button aria-label="Mês anterior" onclick="calMonth.setMonth(calMonth.getMonth()-1);calendar('${id}',false)">${icon('left')}</button><span>${calMonth.toLocaleDateString(loc(),{month:'long',year:'numeric'})}</span><button aria-label="Próximo mês" onclick="calMonth.setMonth(calMonth.getMonth()+1);calendar('${id}',false)">${icon('right')}</button></div><div class="calendar">${Array.from({length:7},(_,i)=>`<span class="week-label">${weekdays()[(start+i)%7]}</span>`).join('')}${calendarCells(calMonth).map(d=>{let k=Core.key(d),v=Core.value(h,k);return `<button class="calday ${v?'done':''} ${k===todayKey()?'today':''} ${d.getMonth()!==calMonth.getMonth()?'dim':''}" ${d>Core.today()?'disabled':''} data-date="${k}" onclick="mark('${id}','${k}')" oncontextmenu="event.preventDefault();noteEditor('${id}','${k}')">${d.getDate()}${v?`<small>• ${v}</small>`:h.notes?.[k]?'<small>✎</small>':''}</button>`}).join('')}</div><p class="hint">Toque para registrar. Segure um dia para adicionar uma nota.</p><button class="secondary" onclick="notes('${id}')">${t('Notas')}</button></div>`,'calendar');document.querySelectorAll('.calday').forEach(el=>{let timer,held=false;el.onpointerdown=()=>{held=false;timer=setTimeout(()=>{held=true;noteEditor(id,el.dataset.date)},550)};el.onpointerup=el.onpointerleave=el.onpointercancel=()=>clearTimeout(timer);el.addEventListener('click',e=>{if(held){e.preventDefault();e.stopImmediatePropagation()}},true)})}
function numberEntry(id,k){let h=get(id);sheet('Registrar valor',`<p>${esc(h.name)} · ${k}</p><input class="field" type="number" id="amount" min="0" max="999999" step="any" value="${Core.value(h,k)}"><button class="primary" style="margin-top:15px" onclick="let v=Number($('#amount').value);if(Number.isFinite(v)&&v>=0&&v<=999999){get('${id}').entries['${k}']=v;persist();closeSheet();render()}">${t('Guardar')}</button>`)}
function notes(id){contextId=id;let h=get(id),ns=Object.entries(h.notes||{}).filter(x=>x[1]).sort((a,b)=>b[0].localeCompare(a[0]));sheet('Notas',`<button class="secondary" onclick="noteEditor('${id}',todayKey())">+ ${t('Adicionar nota')}</button>${ns.length?ns.map(([k,n])=>`<button class="note-row" onclick="noteEditor('${id}','${k}')"><strong>${k}</strong><p>${esc(n)}</p></button>`).join(''):`<p class="hint">${t('Nenhuma nota')}</p>`}`,'notes')}
function noteEditor(id,k){let h=get(id);sheet('Notas',`<p>${esc(h.name)} · ${k}</p><textarea class="field" id="noteText" maxlength="10000" placeholder="Sua nota">${esc(h.notes?.[k]||'')}</textarea><div class="row" style="margin-top:15px"><button class="secondary danger" onclick="get('${id}').notes??={};delete get('${id}').notes['${k}'];persist();notes('${id}')">${icon('trash')}</button><button class="primary" onclick="get('${id}').notes??={};get('${id}').notes['${k}']=$('#noteText').value;persist();notes('${id}')">${t('Guardar')}</button></div>`,'note')}
function shareHabit(id){let h=get(id),s=Core.streak(h);share(`${h.icon} ${h.name}\n${s.total} dias concluídos · sequência de ${s.current} dias\nLoop Dots`);closeSheet()}
function share(s){if(window.Android)Android.share(s);else navigator.clipboard?.writeText(s).then(()=>toast('Copiado'))}
function goalProgress(h){if(!h.goalPeriod||h.goalPeriod==='none')return '';let count=0,label='',goal=Math.max(1,Number(h.goalCount||1)),now=Core.today();if(h.goalPeriod==='day'){count=Core.streak(h).current;label='Meta de sequência diária'}else{label=h.goalPeriod==='week'?'Meta desta semana':'Meta deste mês';let start=h.goalPeriod==='week'?Core.add(now,-((now.getDay()+6)%7)):new Date(now.getFullYear(),now.getMonth(),1,12);for(let d=start;d<=now;d=Core.add(d,1))if(Core.done(h,Core.key(d)))count++}return `<div class="stat wide"><div class="row between"><span>${label}</span><strong>${count} / ${goal}</strong></div><progress max="${goal}" value="${count}" style="width:100%;height:8px;accent-color:${h.color};margin-top:10px"></progress></div>`}
function renderStats(){let h=get(selected)||active()[0];if(!h){$('#app').innerHTML=head('Estatísticas',false)+`<div class="empty">${t('Nenhum hábito ainda')}</div>`;return}selected=h.id;let daily=Core.streak(h),weekly=Core.periodStreak(h,'week'),monthly=Core.periodStreak(h,'month');let metrics=[['Dias concluídos',daily.total,'hash'],['Taxa de conclusão',Core.rate(h)+'%','percent'],['Sequência diária atual',daily.current,'fire'],['Melhor sequência diária',daily.best,'trophy'],['Sequência semanal atual',weekly.current,'star'],['Melhor sequência semanal',weekly.best,'trophy'],['Sequência mensal atual',monthly.current,'calendar'],['Melhor sequência mensal',monthly.best,'trophy']];let metric=([l,v,i])=>`<div class="stat"><div class="stat-label"><span class="stat-icon">${icon(i)}</span>${t(l)}</div><div class="stat-value">${v}</div></div>`;$('#app').innerHTML=head('Estatísticas',false)+`<div style="--hc:${h.color}"><button class="stats-select" onclick="chooseStats()"><span class="avatar" style="width:28px;height:28px;font-size:19px">${esc(h.icon)}</span>${esc(h.name)}${icon('down')}</button><section class="stats">${goalProgress(h)}<div class="stat wide"><div class="year-nav"><button onclick="statYear--;renderStats()">${icon('left')}</button><span>${statYear}</span><button onclick="statYear++;renderStats()">${icon('right')}</button></div>${heatmap(h,statYear)}</div>${metrics.slice(0,2).map(metric).join('')}<div class="stat wide"><div class="row between">${t('Conclusões mensais')}<span style="color:${h.color}">${icon('stats')}</span></div>${chart(h,statYear)}</div>${metrics.slice(2).map(metric).join('')}</section></div>`}
function chart(h,y){let vals=Array(12).fill(0);Object.keys(h.entries||{}).forEach(k=>{let d=Core.date(k);if(d.getFullYear()===y&&Core.done(h,k)&&d<=Core.today())vals[d.getMonth()]++});let max=Math.max(5,...vals),points=vals.map((v,i)=>[25+i*25,140-v/max*110]),curve='M'+points[0].join(',');for(let i=1;i<points.length;i++){let a=points[i-1],b=points[i],mid=(a[0]+b[0])/2;curve+=' C'+mid+','+a[1]+' '+mid+','+b[1]+' '+b.join(',')}return `<svg class="chart" viewBox="0 0 325 175" role="img" aria-label="${t('Conclusões mensais')}: ${vals.join(', ')}"><defs><linearGradient id="fill" x1="0" y1="0" x2="0" y2="1"><stop stop-color="${h.color}" stop-opacity=".5"/><stop offset="1" stop-color="${h.color}" stop-opacity="0"/></linearGradient></defs>${[0,1,2,3,4].map(i=>`<path d="M25 ${140-i*27.5}H305" stroke="#555" stroke-dasharray="3 4" opacity=".4"/><text x="9" y="${143-i*27.5}" stroke="none" fill="#888" font-size="9">${Math.round(i*max/4)}</text>`).join('')}<path d="${curve} L300,140 L25,140 Z" fill="url(#fill)" stroke="none"/><path class="curve" d="${curve}" stroke="${h.color}" stroke-width="2" fill="none"/>${vals.map((v,i)=>`<circle cx="${25+i*25}" cy="${140-v/max*110}" r="2" fill="${h.color}"/><text x="${25+i*25}" y="160" text-anchor="middle" fill="#999" stroke="none" font-size="9">${new Date(y,i,1).toLocaleDateString(loc(),{month:'short'}).slice(0,3)}</text>`).join('')}</svg>`}
function chooseStats(){sheet('Estatísticas',active().map(h=>`<button class="setting" onclick="selected='${h.id}';closeSheet();renderStats()"><span>${esc(h.icon)}</span><span>${esc(h.name)}</span></button>`).join(''))}
const settingsItems=[['Geral','settings','#ffb333','general'],['Tema','sun','#0db9ca','theme'],['Idioma','globe','#ec4899','language'],['Exportar/importar hábitos','backup','#6264ff','backup'],['Hábitos arquivados','archive','#ff743b','archived'],['Reordenar hábitos','week','#e5788c','reorder'],['Configurar widgets','grid','#59b459','widgets'],['Indicar amigos','share','#ab7ce8','invite'],['Website','globe','#91ba51','website'],['Política de Privacidade','help','#d33cff','privacy'],['Termos de Serviço','note','#f5a000','terms'],['Siga-nos','globe','#c4d63b','follow'],['Solicitação de recurso','help','#14b8a6','feature'],['Erro encontrado','settings','#ff4545','bug'],['Avalia-nos','star','#ffcc14','review'],['Enviar feedback','note','#2488ce','feedback'],['Apoiar o desenvolvedor','heart','#e3429f','support'],['Compartilhar aplicativo','share','#a8ce2d','invite']];
function renderSettings(){$('#app').innerHTML=head('Definições',false)+`<div class="settings">${settingsItems.map(([s,ic,c,id])=>`<button class="setting" onclick="setting('${id}')"><span class="bubble" style="background:${c}">${icon(ic)}</span><span>${t(s)}</span></button>`).join('')}<div class="version">Loop Dots v1.0.5</div></div>`}
function setting(id){if(id==='general'){sheet('Geral',`<label>${t('Começo da semana')}</label><select class="field" onchange="state.settings.weekStart=+this.value;persist();render()"><option value="1" ${state.settings.weekStart!==0?'selected':''}>${t('Segunda-feira')}</option><option value="0" ${state.settings.weekStart===0?'selected':''}>${t('Domingo')}</option></select><label class="subheading">${t('Tela inicial')}</label><select class="field" onchange="state.settings.start=this.value;persist()">${[['month','Mês'],['week','Semana'],['year','Ano'],['stats','Estatísticas']].map(([v,l])=>`<option value="${v}" ${(state.settings.start||'month')===v?'selected':''}>${t(l)}</option>`).join('')}</select>${[['haptic','Vibração'],['motion','Animações']].map(([v,l])=>`<button class="setting" onclick="state.settings.${v}=state.settings.${v}===false;persist();appearance();setting('general')"><span>${t(l)}</span><span class="switch ${state.settings[v]!==false?'on':''}"></span></button>`).join('')}`)}else if(id==='theme'){sheet('Tema',`<div class="chips">${[['dark','Escuro'],['light','Claro'],['system','Sistema']].map(([v,l])=>`<button class="chip ${(state.settings.theme||'dark')===v?'selected':''}" onclick="state.settings.theme='${v}';persist();appearance();setting('theme')">${t(l)}</button>`).join('')}</div><h3 class="subheading">Cor de destaque</h3><div class="color-grid">${colors.map(c=>`<button class="swatch" style="background:${c}" onclick="state.settings.accent='${c}';persist();render();setting('theme')"></button>`).join('')}</div>`)}else if(id==='language'){sheet('Idioma',`<button class="setting" onclick="state.settings.lang='pt';persist();closeSheet();render()">Português (Brasil)</button><button class="setting" onclick="state.settings.lang='en';persist();closeSheet();render()">English</button>`)}else if(id==='backup'){sheet('Exportar/importar hábitos',`<p class="hint">Backup completo: hábitos, registros, notas, categorias e preferências. Na importação, hábitos com o mesmo identificador são atualizados; os demais são mantidos.</p><button class="primary" onclick="exportBackup()">${t('Exportar backup')}</button><button class="secondary" style="width:100%;margin-top:12px" onclick="importBackup()">${t('Importar backup')}</button>`)}else if(id==='archived'){let hs=state.habits.filter(h=>h.archived);sheet('Hábitos arquivados',hs.length?hs.map(h=>`<div class="note-row"><strong>${esc(h.icon)} ${esc(h.name)}</strong><div class="row" style="margin-top:10px"><button class="secondary" onclick="get('${h.id}').archived=false;persist();render();setting('archived')">${t('Restaurar')}</button><button class="secondary danger" onclick="deleteHabit('${h.id}')">${icon('trash')}</button></div></div>`).join(''):'<p class="hint">Nenhum hábito arquivado.</p>')}else if(id==='reorder'){sheet('Reordenar hábitos',state.habits.map((h,i)=>`<div class="setting"><span>${esc(h.icon)} ${esc(h.name)}</span><button onclick="reorder(${i},-1)">↑</button><button onclick="reorder(${i},1)">↓</button></div>`).join(''))}else if(id==='widgets'){sheet('Configurar widgets',`<p class="hint">Escolha um formato para adicionar à tela inicial. Depois selecione o hábito e a aparência.</p>${['Pequeno · 2×1','Lista compacta · 4×1','Médio · 4×1','Grade · 3×2','Grade ampla · 4×2'].map((n,i)=>`<button class="setting" onclick="if(window.Android)Android.pinWidget(${i});else toast('Disponível no Android')">${icon('grid')}<span>${n}</span>${icon('plus')}</button>`).join('')}`)}else if(id==='privacy'){sheet('Política de Privacidade','<div class="settings-note"><h3>Seus dados ficam no aparelho</h3><p>Loop Dots armazena hábitos, registros, notas e preferências localmente. Esta versão não contém anúncios, analytics, conta ou servidor de sincronização.</p><p>O backup exportado contém seus registros. Você escolhe onde guardá-lo e com quem compartilhá-lo. Links externos abrem no navegador e seguem as políticas do respectivo serviço.</p><p>Notificações são usadas somente para os lembretes que você configurar. O backup do Android pode copiar os dados conforme as configurações do seu dispositivo.</p><p>Você pode excluir hábitos definitivamente pela lista de arquivados ou apagar os dados do aplicativo nas configurações do Android.</p></div>')}else if(id==='terms'){sheet('Termos de Serviço','<div class="settings-note"><h3>Uso do Loop Dots</h3><p>Aplicativo pessoal para acompanhar hábitos. Os dados registrados e as metas são definidos por você.</p><p>As estatísticas representam os registros locais e não substituem orientação profissional. Mantenha backups para recuperar dados em caso de perda do aparelho ou desinstalação.</p><p>Esta distribuição é disponibilizada pelo repositório LEOStartup/Loop-dots. Não há cobrança ou assinatura nesta versão.</p></div>')}else if(id==='invite'){share('Conheça o Loop Dots — hábitos e widgets para Android. https://github.com/LEOStartup/Loop-dots/releases/latest')}else if(id==='website'||id==='follow'){openUrl('https://github.com/LEOStartup/Loop-dots')}else if(id==='support'){sheet('Apoiar o desenvolvedor',`<p class="settings-note">Você pode apoiar o Loop Dots compartilhando o aplicativo, relatando melhorias ou adicionando uma estrela no GitHub.</p><button class="primary" onclick="openUrl('https://github.com/LEOStartup/Loop-dots')">Abrir projeto</button>`)}else{const title={feature:'Sugestão de recurso',bug:'Relato de erro',review:'Avaliação',feedback:'Feedback'}[id]||'Feedback';sheet(title,`<textarea class="field" id="feedbackText" placeholder="Escreva sua mensagem"></textarea><p class="hint">Será aberto um rascunho no GitHub. Você revisa antes de enviar.</p><button class="primary" onclick="openUrl('https://github.com/LEOStartup/Loop-dots/issues/new?title='+encodeURIComponent('${title}')+'&body='+encodeURIComponent($('#feedbackText').value))">Continuar no GitHub</button>`)}}
function openUrl(url){if(window.Android)Android.openUrl(url);else window.open(url,'_blank','noopener')}
function reorder(i,delta){let j=i+delta;if(j<0||j>=state.habits.length)return;[state.habits[i],state.habits[j]]=[state.habits[j],state.habits[i]];persist();setting('reorder')}
function deleteHabit(id){let h=get(id);sheet('Excluir definitivamente',`<p>Excluir ${esc(h.name)} e todos os seus registros e notas?</p><button class="primary danger" onclick="state.habits=state.habits.filter(h=>h.id!=='${id}');persist();render();setting('archived')">${t('Excluir definitivamente')}</button>`)}
function exportBackup(){if(window.Android)Android.exportBackup();else{let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(state,null,2)],{type:'application/json'}));a.download='LoopDots-backup.json';a.click();URL.revokeObjectURL(a.href)}}
function importBackup(){if(window.Android)Android.importBackup();else{let el=document.createElement('input');el.type='file';el.accept='.json';el.onchange=async()=>onImport(await el.files[0].text());el.click()}}
function onImport(raw){try{let incoming=Core.validate(typeof raw==='string'?JSON.parse(raw):raw);sheet('Importar backup',`<p>${incoming.habits.length} hábitos encontrados. Os registros desses hábitos serão substituídos pelo conteúdo do backup.</p><button class="primary" id="confirmImport">Importar</button>`);$('#confirmImport').onclick=()=>{let map=new Map(state.habits.map(h=>[h.id,h]));incoming.habits.forEach(h=>map.set(h.id,h));state.habits=[...map.values()];state.categories=[...new Set([...state.categories,...(incoming.categories||[])])];state.settings={...state.settings,...incoming.settings};persist();closeSheet();render();toast('Backup importado')};}catch(e){toast('Backup inválido. Nenhum dado foi alterado.')}}
function refreshFromNative(){if(!window.Android||activeSheet==='edit')return;try{state=Core.validate(JSON.parse(Android.load()));state.settings??={};render()}catch(e){toast('Não foi possível atualizar os dados')}}
function openHabit(id){if(get(id))calendar(id)}
function nativeBack(){if(activeSheet){closeSheet();return true}if(page!=='month'){navigate('month');return true}return false}
render();if(loadError)sheet('Dados indisponíveis','<p>Não foi possível ler os registros. Os dados existentes foram preservados. Feche e reabra o aplicativo antes de continuar.</p>');if(window.Android){const id=Android.initialHabit();if(id)setTimeout(()=>openHabit(id),100)}
